import { makeStyles } from '@material-ui/core/styles';

export const useStylesSteppTwo = makeStyles((theme) => ({

    btncontinuar:{
        height:'50px',
        width:'150px',
        textTransform:'initial',
        fontWeight:'700',
        fontSize:'20px'
    }
    
}))